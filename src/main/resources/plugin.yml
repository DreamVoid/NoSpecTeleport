name: ${project.name}
author: DreamVoid
description: ${project.description}
main: me.dreamvoid.nospecteleport.main
version: ${project.version}
api-version: 1.13
prefix: DreamVoidPlugin
webside: ${project.url}
permissions:
  nospecteleport.bypass:
    description: Allow spectator teleport
    default: false
